class ProdutoComum extends Produto {
    public ProdutoComum(String nome, double preco, String tipo, double peso, String codigoBarras, String codigoIdentificacao, String marca) {
        super(nome, preco, tipo, peso, codigoBarras, codigoIdentificacao, marca);
    }
}
